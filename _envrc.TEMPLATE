# edit the HOST_NAME and SITE_URL to your local values
export HOST_NAME=localhost
export SITE_URL=http://${HOST_NAME}

export POSTGRES_DB=urlly
export POSTGRES_USER=urlly
# use the autogenerated POSTGRES_PASSWORD or put in your own
